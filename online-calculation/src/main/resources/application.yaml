management:
  endpoints:
    web:
      exposure:
        include: "*"
app:
  version: 1.0.0

server:
  servlet:
    context-path: /metrics


spring:
  http:
    multipart:
      enabled: true
      max-file-size: 5000MB
      max-request-size: 5000MB
  servlet:
    multipart:
      max-file-size: 5000MB
      max-request-size: 5000MB